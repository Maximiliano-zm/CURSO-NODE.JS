const nombre = 'deadpool';
const real = 'wade winston';

const normal = nombre +' '+ real;
// template string se puede utilizar variables dentro de un string
const template = `maximiliano ${1 + 1}`;


console.log(normal);
console.log(template);

const html = `
<h1> hola </h1>
`;

console.log(html);